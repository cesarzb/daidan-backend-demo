To reset database `ruby db/reset.rb` in the root directory.

To run migrations use `sequel -m db/migrations/ sqlite://db.db` in the root directory.

To seed database `ruby db/seed.rb` in the root directory.